/*
  Создает и заполняет массив random'ными значениями.
  Число элементов массива берется с STDIN и передается
  в вызывающую функцию через входной параметр.
  И число элементов массива, и значения элементов массива
  находятся в пределах unsigned char
*/
unsigned char *mkarr(unsigned char *);
